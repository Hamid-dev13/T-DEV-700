{
  "name": "frontend-auth-styled",
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:coverage:check": "vitest --coverage --run && node -e \"const fs = require('fs'); const data = JSON.parse(fs.readFileSync('coverage/coverage-summary.json', 'utf8')); const coverage = data.total.lines.pct; console.log('📊 Coverage actuel:', coverage.toFixed(2) + '%'); if (coverage < 40) { console.error('❌ Coverage insuffisant! Minimum requis: 40%, actuel:', coverage.toFixed(2) + '%'); process.exit(1); } else { console.log('✅ Coverage OK! Seuil atteint:', coverage.toFixed(2) + '% >= 40%'); }\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@testing-library/jest-dom": "^6.4.2",
    "@testing-library/react": "^14.2.1",
    "@testing-library/user-event": "^14.5.2",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@types/react-router-dom": "^5.3.3",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^1.3.1",
    "@vitest/ui": "^1.3.1",
    "jsdom": "^24.0.0",
    "react-router-dom": "^7.9.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.9.3",
    "vite": "^5.4.11",
    "vitest": "^1.3.1"
  }
}
